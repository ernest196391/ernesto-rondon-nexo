"use client";
import {FormEvent,useState} from "react";
export default function AdminLoginClient(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[busy,setBusy]=useState(false);const[error,setError]=useState("");async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError("");try{const r=await fetch("/api/gestoras/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});const b=await r.json();if(!r.ok||b.role!=="admin")throw new Error(b.error||"Acceso administrador no válido.");window.location.href="/admin";}catch(e){setError(e instanceof Error?e.message:"No pudimos iniciar sesión.")}finally{setBusy(false)}}return <main className="admin-shell"><section className="admin-login-card"><span>ADMINISTRACIÓN</span><h1>Centro de control</h1><p>Acceso exclusivo para administración NEXO.</p><form onSubmit={submit}><label>Correo<input type="email" value={email} onChange={e=>setEmail(e.target.value)} autoComplete="username" required/></label><label>Contraseña<input type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password" required/></label><button disabled={busy}>{busy?"Entrando…":"Entrar al panel"}</button>{error&&<p className="admin-login-error" role="alert">{error}</p>}</form><a href="/impulsa">Volver a Impulsa</a></section></main>}
