import {redirect} from "next/navigation";import Link from "next/link";import {currentCommercialActor} from "../../../lib/commercial/auth";import {adminControlData} from "../../../lib/commercial/admin-control";import AdminNav from "../AdminNav";import {GestoraStatusAction} from "../ControlActions";import "../admin.css";export const dynamic="force-dynamic";export default async function Page(){const actor=await currentCommercialActor();if(!actor||actor.role!=="admin")redirect("/admin/login");const d=await adminControlData();return <main className="admin-shell"><header className="admin-top"><div><img src="/brand/nexo-logo-001g.png" alt="NEXO"/><span>ADMIN</span></div><Link href="/">Ver tienda</Link></header><AdminNav/><div className="admin-wrap"><section className="admin-hero compact"><span>RED</span><h1>Gestoras</h1><p>Estado, tienda, ventas, comisión y enlace atribuible.</p></section><section className="admin-panel"><div className="simple-list">{d.gestoras.map((g:any)=><div key={g.id} style={{alignItems:"flex-start"}}><span><b>{g.publicName}</b><small>{g.userId} · {g.whatsapp||"Sin WhatsApp"}</small><small>Tienda: /g/{g.slug} · Ref: {g.referralCode}</small><small>{g.sales} ventas · {Number(g.availableCommission||0).toFixed(2)} USD disponibles</small></span><div><strong>{g.status}</strong><GestoraStatusAction gestoraId={g.id} status={g.status}/></div></div>)}</div></section></div></main>}
