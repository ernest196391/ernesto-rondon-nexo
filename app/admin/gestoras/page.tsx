import {redirect} from "next/navigation";import Link from "next/link";import {currentCommercialActor} from "../../../lib/commercial/auth";import {adminControlData} from "../../../lib/commercial/admin-control";import AdminNav from "../AdminNav";import {GestoraStatusAction} from "../ControlActions";import "../admin.css";export const dynamic="force-dynamic";export default async function Page({searchParams}:{searchParams?:Promise<{q?:string;estado?:string}>}){const actor=await currentCommercialActor();if(!actor||actor.role!=="admin")redirect("/admin/login");const p=searchParams?await searchParams:{},q=String(p.q||"").trim().toLowerCase(),estado=String(p.estado||"all");const d=await adminControlData();const visible=d.gestoras.filter((g:any)=>(estado==="all"||g.status===estado)&&(!q||`${g.publicName} ${g.userId} ${g.whatsapp} ${g.slug} ${g.referralCode}`.toLowerCase().includes(q)));return <main className="admin-shell"><header className="admin-top"><div><img src="/brand/nexo-logo-001g.png" alt="NEXO"/><span>ADMIN</span></div><Link href="/">Ver tienda</Link></header><AdminNav/><div className="admin-wrap"><section className="admin-hero compact"><span>RED</span><h1>Gestoras</h1><p>Estado, tienda, ventas, comisión y enlace atribuible.</p></section><form className="admin-product-search"><input name="q" defaultValue={q} placeholder="Buscar nombre, correo, WhatsApp o ref"/><select name="estado" defaultValue={estado}><option value="all">Todas</option><option value="active">Activas</option><option value="pending">Pendientes</option><option value="suspended">Suspendidas</option></select><button>Buscar</button></form><section className="admin-panel"><div className="admin-heading"><div><span>RED</span><h2>{visible.length} gestoras</h2></div></div><div className="simple-list">{visible.map((g:any)=><div key={g.id} style={{alignItems:"flex-start"}}><span><Link className="admin-text-link" href={`/admin/gestoras/${g.id}`}><b>{g.publicName}</b></Link><small>{g.userId} · {g.whatsapp||"Sin WhatsApp"}</small><small>Tienda: /g/{g.slug} · Ref: {g.referralCode}</small><small>{g.sales} ventas · {Number(g.availableCommission||0).toFixed(2)} USD disponibles</small></span><div><strong>{g.status}</strong><GestoraStatusAction gestoraId={g.id} status={g.status}/></div></div>)}</div></section></div></main>}
