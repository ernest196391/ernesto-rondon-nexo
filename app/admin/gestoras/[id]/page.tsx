import {redirect} from "next/navigation";import Link from "next/link";import {currentCommercialActor} from "../../../../lib/commercial/auth";import {adminGestoraDetail} from "../../../../lib/commercial/admin-control";import AdminNav from "../../AdminNav";import {GestoraStatusAction,RequestPayout} from "../../ControlActions";import "../../admin.css";export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{id:string}>}){const actor=await currentCommercialActor();if(!actor||actor.role!=="admin")redirect("/admin/login");const {id}=await params;let d;try{d=await adminGestoraDetail(id)}catch{redirect("/admin/gestoras")}const g=d.profile;const available=d.ledger.filter((x:any)=>x.status==="available").reduce((s:number,x:any)=>s+Number(x.amount||0),0);const revenue=d.snapshots.reduce((s:number,x:any)=>s+Number(x.baseAmount||0)+Number(x.markupAmount||0),0);return <main className="admin-shell"><header className="admin-top"><div><img src="/brand/nexo-logo-001g.png" alt="NEXO"/><span>ADMIN</span></div><Link href="/">Ver tienda</Link></header><AdminNav/><div className="admin-wrap"><Link className="admin-back" href="/admin/gestoras">← Gestoras</Link><section className="admin-hero compact"><span>GESTORA</span><h1>{g.publicName}</h1><p>{g.userId} · {g.whatsapp||"Sin WhatsApp"}</p></section><section className="admin-kpis"><article><small>ESTADO</small><strong>{g.status}</strong></article><article><small>VENTAS</small><strong>{d.snapshots.length}</strong></article><article><small>INGRESOS</small><strong>{revenue.toFixed(2)} USD</strong></article><article><small>DISPONIBLE</small><strong>{available.toFixed(2)} USD</strong></article><article><small>PRODUCTOS</small><strong>{d.products.filter((x:any)=>x.status==="active").length}</strong></article></section><section className="admin-panel"><div className="admin-heading"><div><span>CONTROL</span><h2>Cuenta y tienda</h2></div></div><dl className="admin-facts"><div><dt>Tienda</dt><dd><a className="admin-text-link" href={`/g/${g.slug}`} target="_blank">/g/{g.slug}</a></dd></div><div><dt>Referral</dt><dd>{g.referralCode}</dd></div><div><dt>Moneda</dt><dd>{g.defaultCurrency}</dd></div><div><dt>Alta</dt><dd>{new Date(g.createdAt).toLocaleString("es-CU")}</dd></div></dl><GestoraStatusAction gestoraId={g.id} status={g.status}/>{available>0&&<RequestPayout gestoraId={g.id} currency={g.defaultCurrency}/>}</section><section className="admin-home-grid"><article className="admin-panel"><div className="admin-heading"><div><span>VENTAS</span><h2>Pedidos atribuidos</h2></div></div>{d.snapshots.length?<div className="simple-list">{d.snapshots.map((x:any)=><div key={x.orderId}><span><b>Pedido #{x.orderId}</b><small>{new Date(x.createdAt).toLocaleDateString("es-CU")} · margen {Number(x.markupAmount).toFixed(2)}</small></span><strong>{Number(x.earning).toFixed(2)} {x.currency}</strong></div>)}</div>:<div className="admin-empty">Sin ventas atribuidas.</div>}</article><article className="admin-panel"><div className="admin-heading"><div><span>TIENDA</span><h2>Productos y reglas</h2></div></div><div className="simple-list"><div><span><b>Productos seleccionados</b><small>Activos en su storefront</small></span><strong>{d.products.filter((x:any)=>x.status==="active").length}</strong></div><div><span><b>Reglas de precio</b><small>Activas o históricas</small></span><strong>{d.rules.length}</strong></div><div><span><b>Payouts</b><small>Historial de pagos</small></span><strong>{d.payouts.length}</strong></div></div></article></section></div></main>}
