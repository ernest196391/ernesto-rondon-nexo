import {redirect} from "next/navigation";import Link from "next/link";import {currentCommercialActor} from "../../../lib/commercial/auth";import {adminOverview} from "../../../lib/commercial/admin-overview";import AdminNav from "../AdminNav";import "../admin.css";export const dynamic="force-dynamic";export default async function Page(){const actor=await currentCommercialActor();if(!actor||actor.role!=="admin")redirect("/admin/login");const d=await adminOverview();const map=new Map<string,any>();for(const o of d.orders){const key=(o.email||o.phone||o.customer).toLowerCase();const x=map.get(key)||{name:o.customer,email:o.email,phone:o.phone,orders:0,total:0,last:o.date};x.orders++;x.total+=o.total;if(new Date(o.date)>new Date(x.last))x.last=o.date;map.set(key,x)}const clients=[...map.values()].sort((a,b)=>b.total-a.total);return <main className="admin-shell"><header className="admin-top"><div><img src="/brand/nexo-logo-001g.png" alt="NEXO"/><span>ADMIN</span></div><Link href="/">Ver tienda</Link></header><AdminNav/><div className="admin-wrap"><section className="admin-hero compact"><span>CLIENTES</span><h1>Directorio de clientes</h1><p>Derivado de pedidos NEXO, sin crear una base comercial paralela.</p></section><section className="admin-panel"><div className="simple-list">{clients.length?clients.map((c:any,i)=><div key={`${c.email}-${c.phone}-${i}`}><span><b>{c.name}</b><small>{c.phone||"Sin teléfono"} · {c.email||"Sin email"}</small><small>Último pedido: {new Date(c.last).toLocaleDateString("es-CU")}</small></span><strong>{c.orders} pedidos · {c.total.toFixed(2)} USD</strong></div>):<div className="admin-empty">Aún no hay clientes NEXO.</div>}</div></section></div></main>}
