"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
import "../impulsa.css";
export default function Login(){const[key,setKey]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(false),router=useRouter();async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError("");try{const r=await fetch("/api/gestoras/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({accessKey:key})}),d=await r.json();if(!r.ok)throw new Error(d.error);router.replace("/impulsa");router.refresh();}catch(e){setError(e instanceof Error?e.message:"No pudimos iniciar sesión.");}finally{setBusy(false)}}return <main className="impulsa-login"><form onSubmit={submit}><span>NEXO IMPULSA</span><h1>Tu oficina para vender mejor.</h1><p>Entra para administrar tu tienda, tus precios y tus ganancias.</p><label>Código de acceso<input value={key} onChange={e=>setKey(e.target.value)} type="password" autoComplete="current-password" required/></label>{error&&<div role="alert">{error}</div>}<button disabled={busy}>{busy?"Entrando…":"Entrar"}</button><a href="https://wa.me/5354056173">Necesito ayuda</a></form></main>}
