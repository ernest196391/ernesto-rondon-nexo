"use client";
import {useState} from "react";
export default function Page(){const [idea,setIdea]=useState("");const [result,setResult]=useState("");const [loading,setLoading]=useState(false);
async function analyze(){if(!idea.trim())return;setLoading(true);setResult("");try{const r=await fetch("/api/analyze",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({idea})});const d=await r.json();setResult(d.analysis||d.error||"No se pudo analizar.");}catch{setResult("No se pudo conectar con el analizador.");}finally{setLoading(false)}}
return <main className="section tool"><div className="eyebrow">Herramienta gratuita · MVP</div><h1>NEXO Business Analyzer</h1><p className="lead">Describe una idea de negocio. El sistema la someterá a un primer filtro de problema, cliente, monetización, riesgos y validación.</p><textarea value={idea} onChange={e=>setIdea(e.target.value)} placeholder="Ejemplo: Quiero conectar electricistas disponibles con personas que necesitan reparaciones en su casa..."/><button onClick={analyze} disabled={loading}>{loading?"Analizando...":"Analizar mi idea"}</button>{result&&<div className="result">{result}</div>}</main>}
