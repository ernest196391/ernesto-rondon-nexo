import {NextResponse} from "next/server";import {runBusinessAudit} from "../../../../lib/studio/business-audit";
export async function POST(request:Request){try{const body=await request.json() as {answers?:string};if(!body.answers?.trim())return NextResponse.json({error:"Pega las respuestas del formulario para analizar la parte interna."},{status:400});const result=runBusinessAudit(body.answers);return NextResponse.json({ok:!result.privacyBlocked,result},{status:result.privacyBlocked?422:200});}catch{return NextResponse.json({error:"No se pudo analizar el formulario."},{status:400})}}