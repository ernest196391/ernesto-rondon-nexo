import { NextResponse } from "next/server";import { listWooProducts, wooConfigured } from "../../../../lib/commerce/woocommerce";
export const dynamic="force-dynamic";
export async function GET(request:Request){if(!wooConfigured())return NextResponse.json({products:[],configured:false,error:"Catálogo WooCommerce pendiente de credenciales."},{status:503,headers:{"Cache-Control":"no-store"}});const u=new URL(request.url);const products=await listWooProducts({search:u.searchParams.get("search")||undefined,category:u.searchParams.get("category")||undefined,page:Number(u.searchParams.get("page")||1),perPage:Number(u.searchParams.get("perPage")||24)});return NextResponse.json({products,configured:true,storeUrl:process.env.WOOCOMMERCE_URL?.replace(/\/$/,"")},{headers:{"Cache-Control":"s-maxage=60, stale-while-revalidate=300"}});}
